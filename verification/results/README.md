# Verification results

No test has been executed yet.

Create one dated directory or Markdown record per execution. Never overwrite a failed result after a fix; add a rerun and link both records.

Suggested result header:

```text
Test case:
Date/operator:
Hardware revision:
Firmware commit:
Equipment/setup:
Expected result:
Actual result:
Status: PASS / FAIL / BLOCKED
Evidence files:
Anomalies/follow-up:
```
